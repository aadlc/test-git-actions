// duplicated this file to test FILTER_REGEX_INCLUDE env variable
function test(name) {
  console.log(name);
}
const firstName = 'pedro';

const things = ['abc', 'abc', 'abc'];
things.pop();

test(firstName);
